//this is really old code for making the updateDisplay html, found a shorter way        
        
        
        // let image = document.createElement("img");
        // image.src = productImage;
        
        // let cardHeader = document.createElement("ion-card-header");

        // let productTitle = document.createElement("ion-card-title");
        // let brandTitle = document.createElement("ion-card-subtitle");
        // let tagsList = document.createElement("ion-list");
        // let tags = document.createElement("ion-item");
        // let wishlistBtn = document.createElement("ion-button");
        // let otherBtn = document.createElement("ion-button");

        // let description = document.createElement("p");

        

        // productTitle.textContent = productName;
        // brandTitle.textContent = brandName;
        // description.textContent = productDesc;

        // card.appendChild(cardContent);
        // card.appendChild(image);

        // card.appendChild(cardHeader);


        // cardHeader.appendChild(productTitle);
        // cardHeader.appendChild(brandTitle);
        // card.appendChild(wishlistBtn);
        // card.appendChild(otherBtn);

        // cardContent.appendChild(description);


        //not important code
        // cardContent.appendChild(tagsList);

        // for(let i = 0; i < productTags.length; i++){
        //     tags.textContent = productTags[i];
        //     tagsList.appendChild(tags);

        //     // console.log(productTags[i]);
        // }

        // wishlistBtn.id = "wishlist-btn";
        // wishlistBtn.textContent = "wishlist";
        // wishlistBtn.fill = "clear";

        // otherBtn.id = "other-btn";
        // otherBtn.textContent = "wishlist";
        // otherBtn.fill = "clear";

        // outputList.appendChild(card);